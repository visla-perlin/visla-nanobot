apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}
spec:
  selector:
    app: {{ .Values.app.name }}
  ports:
    - port: {{ .Values.webuiPort }}
      targetPort: {{ .Values.webuiPort }}
      name: http-{{ .Values.app.name }}
  type: NodePort
